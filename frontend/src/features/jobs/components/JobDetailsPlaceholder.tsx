import { Section } from "../../../shared/ui";

export function JobDetailsPlaceholder() {
  return (
    <Section
      className="jobs-placeholder jobs-placeholder--details"
      title="Job details"
      description="Select a job to see its progress and URL results."
    />
  );
}
